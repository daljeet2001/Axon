{
  "name": "processor",
  "version": "1.0.0",
  "main": "index.js",
"scripts": {
  "copy-cert": "mkdir -p dist/kafka && cp src/kafka/*.pem dist/kafka/",
  "dev": "tsc -b && npm run copy-cert && node dist/index.js"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.13.0",
    "dotenv": "^17.2.1",
    "kafkajs": "^2.2.4",
    "prisma": "^6.13.0"
  },
  "devDependencies": {
    "@types/node": "^24.2.0"
  }
}
